# Flask Application Environment Variables
# Copy this file to .env for local development and set your values
# For Render deployment, set these in the Render dashboard or via CLI

# Application Settings
APP_NAME=Flask Demo App
APP_VERSION=1.0.0
ENVIRONMENT=development
CUSTOM_MESSAGE=Welcome to our Flask application!

# Flask Configuration
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=False

# Notes:
# - SECRET_KEY: Used for session management and security. Generate a strong random key for production.
# - DEBUG: Set to True for development, False for production.
# - ENVIRONMENT: Can be 'development', 'staging', or 'production'
# - For production (Render), make sure to set a strong SECRET_KEY and DEBUG=False
